services:
  - type: web
    name: dividend-chart-score
    env: python
    plan: free
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run chart_score_app.py --server.port $PORT --server.address 0.0.0.0
